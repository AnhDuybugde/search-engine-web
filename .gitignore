# Python / local tooling (do not commit)
venv/
.venv/
env/
**/__pycache__/
*.py[cod]

# Design skill submodule / local notes (not part of product)
ui-ux-pro-max-skill/
docs-document-management-analysis.md
plan.md

# Legacy upstream Open Notebook clone — not used by web/ runtime
# (Docker + FastAPI + SurrealDB + Ollama). Keep locally if you want
# reference; do not ship or deploy from this monorepo.
open-notebook/

# Local embedding/reranker model weights. Keep these on disk, but do not push
# large model binaries to GitHub.
bge-small-scifact-rrf/**/model.safetensors
bge-small-scifact-rrf/**/*.bin
bge-small-scifact-rrf/**/*.onnx

# Node
node_modules/
web/node_modules/
web/.next/
web/out/
*.tsbuildinfo
next-env.d.ts

# Secrets — never commit
.env
.env.*
!.env.example
web/.env
web/.env.*
!web/.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Logs & coverage
*.log
npm-debug.log*
coverage/
.turbo/

# BEIR raw corpora (large) — seed from scripts/seed-beir-raw.mjs
web/data/
