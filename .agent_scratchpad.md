## Current Goal
- [x] Integrate BM25 + BGE dense + Adaptive RRF retrieval into the Next.js production app.
- [ ] Deploy after production embedding env/project path is confirmed.

## System State
- Production app lives in `web/` and builds on Vercel as a serverless Next.js app.
- Retrieval defaults to `RETRIEVAL_MODE=bm25`; set `RETRIEVAL_MODE=adaptive_rrf` plus embedding env to enable dense fusion.
- Embedding is externalized through `EMBEDDING_PROVIDER`, `EMBEDDING_API_URL`, `EMBEDDING_API_KEY`, and `EMBEDDING_MODEL`.
- Notebook chunk embeddings are cached when upload happens and embedding is configured; missing embeddings fall back to on-demand embedding or BM25 fallback.

## Execution Plan
1. Add env vars to Vercel.
2. Run `web/drizzle/0002_chunk_embeddings.sql` or `npm run db:init`.
3. Deploy from the existing Vercel-linked repo/project.
4. Verify `/api/health` diagnostics and test Web Search + Notebook.

## Context Summary
- Verified locally with `npm test`, `npm run lint`, and `npm run build`.
- Local secrets were moved from repo-root `pyvenv.cfg` to ignored `web/.env.local`; do not commit env files.
- GitHub wiki clone for `search-engine-web.wiki.git` returned `Repository not found`; SEG local `wiki/` was used for pipeline context.
- HF dev mode is configured locally with `EMBEDDING_PROVIDER=huggingface`; smoke test returned 2 BGE-base vectors with dim 768.
- Local `/api/web-search` smoke test with `generateAnswer=false` returned `retrievalMode=adaptive_rrf`, `denseUsed=true`, provider `huggingface`, embedding latency about 2.4s.
- Notebook ask initially failed because Supabase lacked `chunks.embedding_json`; code now falls back to legacy schema when migration is missing.
- Local notebook ask now returns `retrievalMode=adaptive_rrf`, `denseUsed=true`, provider `huggingface`, embedding latency about 18.5s with on-demand BM25-prefiltered chunk embedding.
